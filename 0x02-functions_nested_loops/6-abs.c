#include <stdlib.h>
#include "main.h"

/**
  * _abs - computes the absolute value
  * @r: integer to be tested
  *
  * Return: returns absolute value
  **/

int _abs(int r)
{
	int result;

	result = abs(r);
	return (result);
}
